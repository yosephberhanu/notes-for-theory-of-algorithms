procedure binarySearch(A,K)
	L = 0
    R = n − 1
    location = -1
	while L <= R
        m = floor((L + R) / 2)
        if A[m] < K
            L = m + 1
        else if A[m] > K:
            R = m - 1
        else
            location = m
            break
    end while
    return location
end procedure
