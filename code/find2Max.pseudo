procedure find2Max(A)
  max1 = 0
  max2 = 0
  for i = 1 to n do
    if A[i] > A[max1] then
    	max2 = max1
    	max1 = i
    else if A[i] > A[max2] then
    	max2 = i
    end if
  end for
  return max1,max2
end procedure
