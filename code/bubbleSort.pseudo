procedure bubbleSort(A)
  for i = n-1 down to 0 do
    for j = 0 to i do
      if A[j+1] < A[j] then
        tmp = A[j+1]
        A[j+1] = A[j]
        A[j] = tmp
      end if
    end for
  end for
end procedure
